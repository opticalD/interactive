{
  "name": "bloom-ios",
  "private": true,
  "version": "0.1.0",
  "description": "Native iOS shell for the Bloom mood & habit tracker (Capacitor)",
  "scripts": {
    "build:web": "cd ../habit-mood-tracker && npm run build && rm -rf ../bloom-ios/www && cp -R dist ../bloom-ios/www",
    "sync": "npm run build:web && cap sync ios",
    "add:ios": "cap add ios",
    "open": "cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0",
    "@capacitor/splash-screen": "^6.0.3",
    "@capacitor/status-bar": "^6.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "typescript": "^5.9.3"
  }
}
